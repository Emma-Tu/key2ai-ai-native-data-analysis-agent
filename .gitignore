# Python virtualenv (only used locally to validate YAML)
.venv/
__pycache__/
*.pyc

# OS junk
.DS_Store

# Warehouse inventory — already tracked on remote; fetched locally for reference
/all_tables.csv
