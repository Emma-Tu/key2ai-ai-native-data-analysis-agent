# AUTO-GENERATED skeleton from proto/car_hire.proto. Enrich desc / relationships by hand.
message: AirportTransferItinerary
package: car_hire
source_file: car_hire.proto
group: core
description: "机场接送行程building block（取还地点时间 + 乘客数 + 是否往返）。多数字段已 deprecated。"   # curated (overlay.yaml)
grain: "复用型嵌套消息"   # curated (overlay.yaml)
domain: car_hire

fields:
  - name: sipp
    type: string
    number: 1
    desc: "unavailable in skippy query (should really be removed) see example in CarHireItinerary message"
  - name: pickup_location
    type: commons.Location
    number: 2
    ref: commons.Location   # 外部类型，见 generated/external_refs.yaml
    desc: ""   # TODO 待补充业务含义
  - name: pickup_timestamp
    type: commons.DateTime
    number: 3
    ref: commons.DateTime   # 外部类型，见 generated/external_refs.yaml
    desc: ""   # TODO 待补充业务含义
  - name: dropoff_location
    type: commons.Location
    number: 4
    ref: commons.Location   # 外部类型，见 generated/external_refs.yaml
    desc: ""   # TODO 待补充业务含义
  - name: dropoff_timestamp
    type: commons.DateTime
    number: 5
    ref: commons.DateTime   # 外部类型，见 generated/external_refs.yaml
    desc: ""   # TODO 待补充业务含义
  - name: car_class
    type: string
    number: 6
    desc: "unavailable in skippy query (should really be removed)"
  - name: channel
    type: string
    number: 7
    desc: "field within a redirect that describes where that redirect came from."
  - name: vendor
    type: string
    number: 8
    desc: "unavailable in skippy query (should really be removed) a vendor provides the car for a quote. For example argus provides quotes for Avis' fleet. the website_id may be argus. The vendor will be Avis."
  - name: passenger_number
    type: uint32
    number: 9
    desc: "number of passengers traveling"
  - name: is_return
    type: bool
    number: 10
    desc: "is this a return hire"
