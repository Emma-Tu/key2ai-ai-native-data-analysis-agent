# AUTO-GENERATED skeleton from proto/car_hire.proto. Enrich desc / relationships by hand.
message: SeoLoadAction
package: car_hire
source_file: car_hire.proto
group: core
description: "SEO 页面加载动作building block（页面搜索参数 + 地点参数）。"   # curated (overlay.yaml)
grain: "复用型嵌套消息"   # curated (overlay.yaml)
domain: car_hire

fields:
  - name: search_parameter
    type: SearchParameter
    number: 1
    ref: SearchParameter
    desc: ""   # TODO 待补充业务含义
  - name: location_parameter
    type: CarHireSeoLocationParameter
    number: 2
    ref: CarHireSeoLocationParameter
    desc: "not required when page_type is one of CAR_HIRE_ALL_xxx, CAR_HIRE_FAQ and CAR_HIRE_PROVIDER"

nested_messages:
  - name: SearchParameter
    fields:
      - name: country_name
        type: string
        number: 1
        desc: "required when page_type is CAR_HIRE_COUNTRIES"
      - name: provider_name
        type: string
        number: 2
        desc: "required when page_type is CAR_HIRE_PROVIDER"
      - name: airport_name
        type: string
        number: 3
        desc: "required when page_type is CAR_HIRE_AIRPORT"
      - name: location_name
        type: string
        number: 4
        desc: "required when page_type is CAR_HIRE_LOCATION"
