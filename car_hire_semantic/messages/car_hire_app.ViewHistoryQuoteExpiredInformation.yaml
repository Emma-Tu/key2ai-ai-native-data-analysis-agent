# AUTO-GENERATED skeleton from proto/car_hire_app.proto. Enrich desc / relationships by hand.
message: ViewHistoryQuoteExpiredInformation
package: car_hire_app
source_file: car_hire_app.proto
group: core
description: "浏览历史报价过期building block（日期是否仍有效但报价已失效）。"   # curated (overlay.yaml)
grain: "复用型嵌套消息"   # curated (overlay.yaml)
domain: car_hire

fields:
  - name: is_date_valid
    type: bool
    number: 1
    desc: "True if the date was ok, but offer was gone"
