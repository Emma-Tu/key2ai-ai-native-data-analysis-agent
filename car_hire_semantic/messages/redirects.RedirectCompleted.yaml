# AUTO-GENERATED skeleton from proto/redirects.proto. Enrich desc / relationships by hand.
message: RedirectCompleted
package: redirects
source_file: redirects.proto
group: conversion
description: "跳转完成事件（跳转成功落地的确认）。"   # curated (overlay.yaml)
grain: "一行 = 一次完成的跳转"   # curated (overlay.yaml)
domain: car_hire

fields:
  - name: header
    type: commons.LightHeader
    number: 1
    ref: commons.LightHeader   # 外部类型，见 generated/external_refs.yaml
    desc: "The light event header"
  - name: client_details
    type: commons.ClientDetails
    number: 2
    ref: commons.ClientDetails   # 外部类型，见 generated/external_refs.yaml
    desc: "IP address, user agent, browser details,..."
  - name: redirect_id
    type: string
    number: 3
    desc: "The same as the ID generated by Skippy contained in Redirect.response.redirect_id"
  - name: target_url
    type: string
    number: 4
    desc: "The URL where the user is going to be redirected to"
  - name: is_default_target
    type: bool
    number: 5
    desc: "Whether or not this is the default deeplink fallback target URL"
  - name: pre_redirect_id
    type: string
    number: 6
    desc: "The pre redirect id - used for identification before the actual redirect"
